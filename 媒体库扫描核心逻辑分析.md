# Koel 媒体库扫描核心逻辑分析

## 一、扫描状态与文件路径的映射方式

### 1.1 扫描入口与架构

Koel 的媒体库扫描采用**分层架构**设计，核心入口为 `ScanCommand` 命令：

**扫描命令入口** (`app/Console/Commands/ScanCommand.php:52-79`)
- 支持两种扫描模式：全量扫描 (`scanMediaPath`) 和单记录扫描 (`scanSingleRecord`)
- 支持并行扫描（通过 `--jobs` 参数指定工作进程数）
- 扫描配置通过 `ScanConfiguration` 值对象封装

**扫描器层次结构**：
```
ScanCommand
    ├── DirectoryScanner       # 目录扫描器（全量扫描）
    │   ├── SequentialScanStrategy    # 顺序扫描策略
    │   └── ParallelScanStrategy      # 并行扫描策略
    │       └── ScanChunkCommand      # 子进程扫描命令
    ├── WatchRecordScanner     # 监听记录扫描器（增量同步）
    └── IndividualFileHandler  # 单个文件处理器
        ├── FileScanner        # 文件元数据提取器
        └── SongService        # 歌曲服务
```

### 1.2 文件收集与路径映射

**文件收集机制** (`app/Services/Scanners/Scanner.php:19-30`)

使用 `Symfony\Component\Finder\Finder` 收集音频文件：
- 支持的音频格式由 `collect_accepted_audio_extensions()` 定义
- 可配置忽略隐藏文件（`ignore_dot_files`）
- 自动跟随符号链接

**文件路径与数据库记录的映射** (`app/Repositories/SongRepository.php:48-51`)

```php
public function findOneByPath(string $path): ?Song
{
    return Song::query()->where('path', $path)->first();
}
```

- **主键**：文件系统的绝对路径作为唯一标识
- **查询效率**：通过 `path` 字段索引快速定位
- **适用场景**：增量扫描时判断文件是否已存在

### 1.3 扫描状态管理

**扫描结果类型** (`app/Values/Scanning/ScanResult.php`)

使用 `ScanResultType` 枚举定义三种状态：
- `SUCCESS`：文件扫描成功，新建或更新了记录
- `SKIPPED`：文件未修改，跳过处理
- `ERROR`：文件扫描失败

**扫描状态流转** (`app/Services/Scanners/IndividualFileHandler.php:20-36`)

```
文件路径
    ↓
查询数据库是否存在记录
    ├─ 不存在 → 提取元数据 → 创建记录 → SUCCESS
    └─ 存在
        ├─ 未修改且非强制扫描 → SKIPPED
        └─ 已修改或强制扫描 → 提取元数据 → 更新记录 → SUCCESS
```

**修改检测机制** (`app/Models/Song.php:229-234`)

```php
public function isFileModified(int $lastModified): bool
{
    return $this->mtime !== $lastModified;
}
```

- 通过比较文件的 `mtime`（修改时间戳）判断是否变更
- 简单高效，适合大规模媒体库
- 可通过 `--force` 参数强制重新扫描
- 注意：代码注释提到 "comparing the stored hash or mtime"，但实际仅使用 mtime，hash 字段未用于修改检测

### 1.4 并行扫描策略

**并行扫描实现** (`app/Services/Scanners/Strategies/ParallelScanStrategy.php`)

1. **任务分片**：将文件列表分成 N 个 chunk（N = 工作进程数）
2. **进程管理**：为每个 chunk 启动一个独立的 `artisan koel:scan:chunk` 子进程
3. **结果收集**：通过临时 JSON 文件传递扫描路径，子进程逐行输出扫描结果
4. **实时反馈**：父进程通过非阻塞 IO 实时读取子进程输出并更新进度

---

## 二、元数据到模型各字段的转换路径

### 2.1 元数据提取层

**元数据提取器** (`app/Services/Scanners/FileScanner.php:19-41`)

使用 `getID3` 库分析音频文件，提取流程：
1. 调用 `getID3->analyze()` 获取原始元数据
2. 校验播放时长，空文件直接报错
3. 调用 `CopyTagsToComments()` 标准化标签格式
4. 尝试读取同目录下的 LRC 歌词文件

**标签优先级** (`app/Values/Scanning/ScanInformation.php:37-42`)

```php
$tags = array_merge(
    Arr::get($info, 'tags.id3v1', []),       // ID3v1 (优先级最低)
    Arr::get($info, 'tags.id3v2', []),       // ID3v2
    Arr::get($info, 'comments', []),         // 通用注释
    Arr::get($info, 'tags.vorbiscomment', []), // Vorbis 注释 (OGG/FLAC)
);
```

- 后合并的数组覆盖先合并的，实现优先级控制
- 支持多种音频格式的标签标准

### 2.2 元数据封装层

**ScanInformation 值对象** (`app/Values/Scanning/ScanInformation.php`)

封装从音频文件提取的所有元数据：

| 字段 | 来源标签 | 说明 |
|------|----------|------|
| `title` | `title` | 歌曲标题，默认取文件名 |
| `albumName` | `album` | 专辑名，默认 "Unknown Album" |
| `artistName` | `artist` | 艺术家名，默认 "Unknown Artist" |
| `albumArtistName` | `albumartist`/`album_artist`/`band` | 专辑艺术家 |
| `track` | `track`/`tracknumber` | 曲目号 |
| `disc` | `discnumber`/`part_of_a_set` | 碟号，默认 1 |
| `year` | `year` | 发行年份 |
| `genre` | `genre` | 流派 |
| `lyrics` | `unsynchronised_lyric`/`lyrics` | 歌词 |
| `length` | `playtime_seconds` | 播放时长（秒） |
| `cover` | `cover`/`picture` | 专辑封面 |
| `path` | 文件系统 | 绝对路径 |
| `hash` | `File::hash()` | 文件内容哈希 |
| `mTime` | `get_mtime()` | 修改时间戳 |
| `mimeType` | `mime_type` | MIME 类型 |
| `fileSize` | `File::size()` | 文件大小（字节） |

**特殊处理逻辑**：
- 合辑识别：如果 `part_of_a_compilation` 标签存在且无专辑艺术家，自动设为 "Various Artists"
- 编码修复：所有标签经过 `TagFixer::fix()` 处理乱码问题
- HTML 实体解码：标题、专辑名等字段调用 `html_entity_decode()`

### 2.3 模型转换层

**歌曲创建/更新流程** (`app/Services/SongService.php:216-290`)

```
ScanInformation
    ↓
1. 按路径查询已有 Song 记录
    ↓
2. 解析 Artist
    ├─ 歌曲艺术家：Artist::getOrCreate(owner, artistName)
    └─ 专辑艺术家：优先使用 albumArtistName，否则回退到歌曲艺术家
    ↓
3. 解析 Album：Album::getOrCreate(albumArtist, albumName)
    ↓
4. 处理专辑封面
    ├─ 已有有效封面 → 跳过
    ├─ 元数据包含封面 → 存储封面文件
    └─ 元数据无封面 → 尝试从同级目录读取 cover.jpg 等
    ↓
5. 准备 Song 数据
    ├─ 移除临时字段 (album/artist/albumartist/cover)
    ├─ 设置外键 (album_id, artist_id)
    ├─ 设置冗余字段 (album_name, artist_name) 用于性能优化
    └─ 设置可见性 (is_public)
    ↓
6. 写入数据库
    ├─ 新记录 → create() 并设置 owner_id
    └─ 已有记录 → update() 保留原 owner
    ↓
7. 同步流派：syncGenres(genre)
    ↓
8. 补充专辑年份（如专辑无年份）
    ↓
9. 异步提取文件夹结构（ExtractSongFolderStructureJob）
```

**艺术家/专辑缓存策略** (`app/Services/SongService.php:292-310`)

使用 `ScannerCacheStrategy` 缓存艺术家和专辑实例，避免重复查询：
- 缓存键：`__METHOD__ + user_id + name`
- 作用域：单次扫描请求内有效

### 2.4 冗余字段设计

Song 模型包含 `album_name` 和 `artist_name` 冗余字段：
- **目的**：避免查询歌曲列表时的 JOIN 操作
- **维护**：扫描时同步更新，编辑时通过 `SongService::updateSong()` 同步
- **权衡**：以存储空间换取查询性能

---

## 三、文件删除或移动后数据库记录的同步机制

### 3.1 全量扫描后的失效路径清理机制

**⚠️ 前提边界：MediaScanCompleted 清理链路仅覆盖全量扫描，不覆盖单记录扫描**

**触发位置代码证据**：
- `MediaScanCompleted` 事件仅在 `DirectoryScanner::scan()` 方法中触发 (`app/Services/Scanners/DirectoryScanner.php:46`)
- `DirectoryScanner` 仅在全量扫描路径 `scanMediaPath()` 中被调用 (`app/Console/Commands/ScanCommand.php:103`)
- 单记录扫描路径 `scanSingleRecord()` 直接调用 `WatchRecordScanner::scan()` (`app/Console/Commands/ScanCommand.php:127`)，**不经过 DirectoryScanner**，因此不会触发 MediaScanCompleted

**扫描模式与清理链路对比**：

| 扫描模式 | 触发入口 | 使用扫描器 | 是否触发 MediaScanCompleted | 清理机制 |
|---------|---------|----------|-------------------------|---------|
| **全量扫描** | `php artisan koel:scan`（无 record 参数） | `DirectoryScanner` | ✅ 是 | 触发完整清理链路：失效路径清理 + 空库清理 + 日志写入 |
| **单记录扫描** | `php artisan koel:scan {record}`（带 inotify 记录） | `WatchRecordScanner` | ❌ 否 | 仅处理单个文件的增删，无批量清理 |

**⚠️ 重要事实校正：MediaScanCompleted 监听器是异步队列执行，非严格顺序**

通过代码核查发现，三个监听器**全部实现了 `ShouldQueue` 接口**：

| 监听器类 | 是否实现 ShouldQueue |
|---------|-------------------|
| `DeleteNonExistingRecordsPostScan` | ✅ 是（第 12 行） |
| `PruneLibrary` | ✅ 是（第 8 行） |
| `WriteScanLog` | ✅ 是（第 11 行） |

**事件注册** (`app/Providers/EventServiceProvider.php:47-51`)
```php
MediaScanCompleted::class => [
    DeleteNonExistingRecordsPostScan::class,  // 清理失效路径
    PruneLibrary::class,                       // 清理空专辑/艺术家
    WriteScanLog::class,                       // 写入扫描日志
],
```

**队列异步执行的技术细节**：

Laravel 队列的工作机制决定了监听器的执行特性：
1. **事件触发时**：Laravel 遍历 `EventServiceProvider` 中注册的监听器数组，为每个实现 `ShouldQueue` 的监听器创建一个队列任务
2. **任务入队**：三个任务按数组顺序被推入队列，但这只是入队顺序，不一定是执行顺序
3. **执行调度**：队列工作进程（worker）从队列中拉取任务时，执行顺序取决于队列连接模式、worker 数量、任务优先级等多种因素

---

**按队列连接模式的执行顺序与一致性影响分析**：

项目默认队列连接为 `database` (`config/queue.php:15`)，支持多种连接模式，不同模式下的执行特性存在差异：

#### 模式 1：sync 连接（同步执行）
```dotenv
QUEUE_CONNECTION=sync
```
- **执行顺序**：✅ **严格按照数组顺序执行**，`DeleteNonExistingRecordsPostScan` → `PruneLibrary` → `WriteScanLog`
- **调度机制**：在当前请求/进程内同步执行，不使用后台队列
- **一致性影响**：
  - 数据一致性最好，因为先删除失效歌曲，再清理空专辑/艺术家
  - 阻塞扫描进程，扫描完成后才会依次执行清理任务
- **适用场景**：开发环境、小型媒体库、对一致性要求较高的场景

#### 模式 2：database 连接（默认配置）
```dotenv
QUEUE_CONNECTION=database
```
- **执行顺序**：⚠️ **通常按入队顺序执行，但不保证**，单 worker 时接近顺序执行，多 worker 时可能出现交错
- **调度机制**：任务存储在 `jobs` 表中，worker 轮询拉取，先入队的任务通常先被执行
- **一致性影响**：
  - 单 worker 场景下，大概率能保持正确顺序，一致性较好
  - 多 worker 场景下，可能出现 `PruneLibrary` 先于删除任务执行的情况，导致部分空专辑未能清理
  - 竞态条件存在但概率相对可控
- **适用场景**：中小型部署、单 worker 配置

#### 模式 3：redis / beanstalkd 连接（高性能队列）
```dotenv
QUEUE_CONNECTION=redis
# 或
QUEUE_CONNECTION=beanstalkd
```
- **执行顺序**：❌ **不保证执行顺序**，高并发场景下顺序不确定性增加
- **调度机制**：专业队列服务，支持多 worker 并行消费，任务执行顺序受网络延迟、worker 负载等因素影响
- **一致性影响**：
  - 竞态条件出现概率相对较高
  - `PruneLibrary` 可能在失效歌曲删除前运行，导致空专辑残留
  - 多次全量扫描后最终会收敛到一致状态
- **适用场景**：大型媒体库、高并发部署、多 worker 配置

#### 模式 4：sqs 连接（AWS 托管队列）
```dotenv
QUEUE_CONNECTION=sqs
```
- **执行顺序**：❌ **完全不保证顺序**，SQS 标准队列是 best-effort 顺序
- **调度机制**：AWS SQS 分布式队列，天生不保证严格顺序
- **一致性影响**：
  - 竞态条件出现概率最高
  - 依赖最终一致性模型
- **适用场景**：云原生部署、AWS 环境

---

**WriteScanLog 的独立性**：
`WriteScanLog` 监听器只负责写入扫描日志，不修改任何业务数据，因此与其他两个监听器之间没有数据依赖关系，执行顺序不影响系统数据一致性。

**跨模式的通用结论**：
- ⚠️ **数组顺序 ≠ 执行顺序**：除 sync 模式外，其他队列模式都不能保证严格的执行顺序
- ⚠️ **竞态条件可能存在**：`PruneLibrary` 可能在失效歌曲删除前运行，导致部分空专辑残留
- ✅ **最终一致性**：无论哪种模式，多次全量扫描后都会逐渐收敛到一致状态
- 💡 **优化建议**：如果对一致性要求较高，可考虑使用 sync 队列或单 worker 配置，或者在 `DeleteNonExistingRecordsPostScan` 完成后手动触发 `PruneLibrary`

**失效路径清理核心逻辑** (`app/Listeners/DeleteNonExistingRecordsPostScan.php:18-30`)

```php
public function handle(MediaScanCompleted $event): void
{
    $paths = $event
        ->results
        ->valid()  // 仅包含 SUCCESS 和 SKIPPED 的结果
        ->map(static fn (ScanResult $result) => $result->path)
        ->merge($this->songRepository->getAllStoredOnCloud()->pluck('path'))
        ->toArray();

    Song::deleteWhereValueNotIn($paths, 'path', static function (Builder $builder): Builder {
        return $builder->whereNull('podcast_id'); // 排除播客节目
    });
}
```

**清理算法详解**：
1. 收集本次扫描中所有有效路径（SUCCESS + SKIPPED）
2. 合并云存储歌曲路径（云存储文件不在本地扫描范围内，避免误删）
3. 调用 `deleteWhereValueNotIn()` 删除数据库中路径不在白名单内的所有本地歌曲记录
4. 排除播客节目（`podcast_id IS NOT NULL`）

**批量删除实现** (`app/Models/Concerns/SupportsDeleteWhereValueNotIn.php:22-52`)

解决 SQL IN 子句 65535 元素限制问题：
```php
public static function deleteWhereValueNotIn(
    array $values,
    ?string $field = null,
    ?Closure $queryModifier = null,
): void {
    $maxChunkSize = DB::getDriverName() === 'sqlite' ? 999 : 65_535;

    if (count($values) <= $maxChunkSize) {
        // 路径数量在限制内，直接 DELETE WHERE NOT IN
        self::deleteAndUnsearch($queryModifier(static::query())->whereNotIn($field, $values));
        return;
    }

    // 路径数量超限，先查询所有ID再取差集
    $allIds = static::query()->select($field)->get()->pluck($field)->all();
    $deletableIds = array_diff($allIds, $values);

    if (count($deletableIds) < $maxChunkSize) {
        self::deleteAndUnsearch($queryModifier(static::query())->whereIn($field, $deletableIds));
        return;
    }

    // 待删除数量仍超限，分批删除
    static::deleteByChunk($deletableIds, $maxChunkSize, $field);
}
```

### 3.2 监听删除事件与全量扫描清理的职责边界

| 清理机制 | 触发时机 | 处理范围 | 精度 | 适用场景 |
|---------|---------|---------|------|---------|
| **WatchRecordScanner** | 文件系统事件实时触发 | 单个文件或单个目录 | 精确删除 | inotify 监听到的实时文件操作 |
| **DeleteNonExistingRecordsPostScan** | 全量扫描完成后（队列异步） | 整个媒体库 | 批量清理 | 监听漏事件、手动删除文件、移动文件等 |
| **PruneLibrary** | MediaScanCompleted / LibraryChanged 事件（队列异步） | 整个媒体库 | 级联清理 | 清理因歌曲删除而变空的专辑和艺术家 |

**WatchRecordScanner 的职责** (`app/Services/Scanners/WatchRecordScanner.php`)
- 响应 inotify 事件：`DELETE`、`MOVED_FROM`、`MOVED_TO`、`CLOSE_WRITE`、`CREATE`
- 精确处理单个文件或目录的增删改
- 只处理监听到的事件，不做全库一致性检查
- 实时性优先，适合日常增量同步

**DeleteNonExistingRecordsPostScan 的职责** (`app/Listeners/DeleteNonExistingRecordsPostScan.php`)
- 全量扫描后的最终一致性保证
- 处理所有监听漏处理的场景：
  - 监听服务停机期间的文件变更
  - 手动删除/移动文件（未通过 inotify 监听）
  - 跨文件系统移动（inotify 无法跨设备追踪）
  - 数据库导入/导出导致的数据不一致

**PruneLibrary 的职责** (`app/Services/LibraryManager.php:16-43`)
- 清理没有歌曲的空专辑：`LEFT JOIN songs WHERE songs.album_id IS NULL`
- 清理没有歌曲也没有专辑的空艺术家
- 作为二级清理步骤，不直接处理歌曲记录

**⚠️ 重要事实校正：WatchRecordScanner 删除时不触发 LibraryChanged**

通过代码核查发现：
- `WatchRecordScanner::handleDeletedFileRecord()` 直接调用 `$song->delete()`（第 61 行）
- `WatchRecordScanner::handleDeletedDirectoryRecord()` 直接调用 `Song::query()->inDirectory($path)->delete()`（第 79 行）
- **两者都不会触发 `LibraryChanged` 事件**
- 只有 `SongService::deleteSongs()` 会显式触发 `event(new LibraryChanged())`（第 213 行）

这意味着：
- ✅ 通过 UI/API 调用 `SongService::deleteSongs()` 删除的歌曲 → 触发 PruneLibrary 清理空专辑
- ❌ 通过 inotify 监听删除的歌曲 → 不会触发 PruneLibrary，空专辑/艺术家会残留
- ✅ 全量扫描完成后 → 通过 `MediaScanCompleted` 事件链触发 PruneLibrary（异步队列）

### 3.3 文件移动丢失历史数据的真实原因

#### 3.3.1 song_id 级联约束的真实来源与演进

通过数据库迁移文件核查，各关联表的 `song_id` 外键约束定义如下：

| 表名 | 迁移文件 | 约束定义 | 级联删除 |
|------|---------|---------|---------|
| **interactions** | `2015_11_23_074733_create_interactions_table.php:22` | `$table->foreign('song_id')->references('id')->on('songs')->onDelete('cascade')` | ✅ 是 |
| **playlist_song** | `2015_11_23_082854_create_playlist_song_table.php:19` | `$table->foreign('song_id')->references('id')->on('songs')->onDelete('cascade')` | ✅ 是 |
| **genre_song** | `2025_06_25_103531_add_genres_table.php:23` | `$table->foreign('song_id')->references('id')->on('songs')->cascadeOnDelete()` | ✅ 是 |
| **favorites** | `2025_07_18_105237_favorites.php:19-20` | 无 `song_id` 外键，使用多态关联 `favoriteable_id` + `favoriteable_type` | ❌ 否 |

**关键事实**：
- `interactions`、`playlist_song`、`genre_song` 三张表从创建之初就明确定义了 `song_id` 的 `ON DELETE CASCADE` 约束
- `favorites` 表采用多态关联设计，**没有数据库级的外键约束**，只有 `user_id` 有外键约束

#### 3.3.2 文件移动的完整数据丢失链路

当用户将 `/media/album1/song.mp3` 移动到 `/media/album2/song.mp3` 时：

```
第一步：MOVED_FROM 事件（源路径 /media/album1/song.mp3）
    ↓
WatchRecordScanner::handleDeletedFileRecord("/media/album1/song.mp3")
    ↓
Song::where('path', '/media/album1/song.mp3')->first()?->delete()
    ↓
├─ 数据库级联删除（自动执行）：
│   ├─ interactions 表：DELETE FROM interactions WHERE song_id = 'xxx'
│   ├─ playlist_song 表：DELETE FROM playlist_song WHERE song_id = 'xxx'
│   └─ genre_song 表：DELETE FROM genre_song WHERE song_id = 'xxx'
├─ 多态关联失效（无自动删除）：
│   └─ favorites 表：记录仍然存在，但 favoriteable_id 指向不存在的歌曲 → 孤儿记录
└─ ❌ 不触发 LibraryChanged 事件
    └─ 空专辑/艺术家不会被即时清理

第二步：MOVED_TO 事件（目标路径 /media/album2/song.mp3）
    ↓
WatchRecordScanner::handleNewOrModifiedFileRecord("/media/album2/song.mp3")
    ↓
SongRepository::findOneByPath("/media/album2/song.mp3") → 不存在
    ↓
创建全新的 Song 记录，生成新的 UUID
    ↓
所有关联数据从零开始：
    ├─ play_count = 0
    ├─ 无播放列表关联
    └─ 无流派关联
```

#### 3.3.3 收藏数据消失的真实原因

**⚠️ 重要事实校正：收藏列表不使用 whereHasMorph，过滤完全通过 INNER JOIN 实现**

**whereHasMorph 的使用边界核查**：
通过全代码库搜索确认，项目中**没有任何 PHP 代码使用 `whereHasMorph` 方法**。该方法仅在分析文档中被提及，属于对实现机制的误解。

**实际使用边界对比**：
| 方法 | 项目中是否使用 | 适用场景 | 对孤儿记录的处理 |
|------|---------------|---------|----------------|
| `whereHasMorph` | ❌ 完全未使用 | 从多态关联表出发查询存在关联的主表记录 | 通过 EXISTS 子查询过滤 |
| `INNER JOIN`（当前实现） | ✅ 唯一使用方式 | 从主表（songs）出发查询有关联的记录 | 通过内连接自动排除 |

**真实查询链路** (`app/Repositories/SongRepository.php:150-156`)：
```php
public function getFavorites(?User $scopedUser = null, ?PlayableType $type = null): Collection
{
    return Song::query(type: $type, user: $scopedUser ?? $this->auth->user())
        ->withUserContext(favoritesOnly: true)
        ->orderBy('favorites.position')
        ->get();
}
```

**`withUserContext` 调用链** (`app/Builders/SongBuilder.php:110-119`)：
```php
public function withUserContext(
    bool $includeFavoriteStatus = true,
    bool $favoritesOnly = false,
    bool $includePlayCount = true,
): self {
    return $this
        ->accessible()
        ->when($includeFavoriteStatus, static fn (self $query) => $query->withFavoriteStatus($favoritesOnly))
        ->when($includePlayCount, static fn (self $query) => $query->withPlayCount());
}
```

**`withFavoriteStatus` 实际实现** (`app/Builders/FavoriteableBuilder.php:25-41`)：
```php
public function withFavoriteStatus(bool $favoritesOnly = false): static
{
    // 当 favoritesOnly = true 时，使用 INNER JOIN 而非 LEFT JOIN
    $joinMethod = $favoritesOnly ? 'join' : 'leftJoin';

    $this->$joinMethod('favorites', function (JoinClause $join): void {
        $joinColumn = $this->model->getTable() . '.' . $this->model->getKeyName();

        $join->on('favorites.favoriteable_id', $joinColumn)->where(
            'favorites.favoriteable_type',
            $this->getModel()->getMorphClass(),
        )->where('favorites.user_id', $this->user->id);
    });

    $this->addSelect(DB::raw('CASE WHEN favorites.created_at IS NULL THEN false ELSE true END AS favorite'));

    return $this;
}
```

**真实查询链路与过滤机制详解**：

```
SongRepository::getFavorites()
    ↓ 第 152-155 行
Song::query()
    ↓ 传入 favoritesOnly: true
SongBuilder::withUserContext(favoritesOnly: true)
    ↓ 第 402 行调用
FavoriteableBuilder::withFavoriteStatus(favoritesOnly: true)
    ↓ 第 27 行
$joinMethod = 'join';  // INNER JOIN，而非 LEFT JOIN
    ↓ 第 29-36 行
INNER JOIN favorites ON 
    favoriteable_id = songs.id 
    AND favoriteable_type = 'playable'
    AND user_id = ?
    ↓
自动过滤：
  - 无收藏记录的歌曲 → 被 INNER JOIN 排除
  - 收藏指向已删除歌曲 → 查询从 songs 出发，永远不会匹配到
    ↓
orderBy('favorites.position') → get()
```

**过滤机制真相**：
1. **查询方向**：从 `songs` 表出发，INNER JOIN `favorites` 表（关键：不是从 favorites 出发）
2. **连接类型**：当 `favoritesOnly = true` 时使用 `INNER JOIN`，`favoritesOnly = false` 时使用 `LEFT JOIN`
3. **自动过滤**：INNER JOIN 自动排除没有匹配收藏记录的歌曲
4. **孤儿记录不可见**：如果歌曲被删除，在当前查询链路下，favorites 表中的孤儿记录**不会被现有查询接口返回**，因为查询起点是 songs 表，已删除的歌曲不在结果集中
5. **数据残留**：`favorites` 表中仍然存在指向已删除歌曲的孤儿记录，占用数据库空间但不可见，建议定期清理

**查询方向对比**：
| 查询方向 | SQL 模式 | 孤儿记录可见性 |
|---------|---------|-------------|
| songs JOIN favorites（当前实现） | `SELECT * FROM songs JOIN favorites ON ...` | ❌ 不可见 |
| favorites LEFT JOIN songs | `SELECT * FROM favorites LEFT JOIN songs ON ...` | ✅ 可见（关联字段为 NULL） |
| whereHasMorph | `SELECT * FROM favorites WHERE EXISTS (...)` | ❌ 不可见 |

#### 3.3.4 hash 字段的未充分利用

代码中虽然计算并存储了文件内容哈希 (`ScanInformation.php:80`)：
```php
'hash' => File::hash($path),
```

但**从未用于识别移动的文件**：
- `findOneByPath()` 只查询 path 字段
- `isFileModified()` 只比较 mtime
- `DeleteNonExistingRecordsPostScan` 清理时不做哈希匹配

#### 3.3.5 真实原因总结

文件移动后历史数据丢失的四层原因：

| 层面 | 原因 | 影响 |
|------|------|------|
| **标识层** | 使用文件系统路径作为歌曲唯一标识，而非文件内容哈希 | 移动后路径改变，系统认为是两首不同歌曲 |
| **数据库层** | interactions/playlist_song/genre_song 有 ON DELETE CASCADE 约束 | 旧歌曲记录删除时，播放历史、播放列表关联、流派关联被级联删除 |
| **应用层** | 未实现"按哈希查找旧记录 → 更新路径 → 迁移关联数据"的逻辑 | 无法识别移动的文件，无法保留用户数据 |
| **关联层** | favorites 使用多态关联，查询从 songs 表出发 INNER JOIN | 收藏记录变成孤儿，查询时被内连接自动过滤，用户感知为"消失" |

### 3.4 inotify 事件解析

**Inotify 事件映射** (`app/Values/WatchRecord/InotifyWatchRecord.php:30-45`)

| inotify 事件 | Koel 分类 | 处理逻辑 |
|-------------|----------|---------|
| `DELETE` | 已删除 | 删除数据库记录 |
| `MOVED_FROM` | 已删除 | 记录被移出监听目录，视为删除 |
| `CLOSE_WRITE` | 新建/修改 | 文件写入完成，触发扫描 |
| `CREATE` | 新建/修改 | 新文件创建，触发扫描 |
| `MOVED_TO` | 新建/修改 | 文件被移入监听目录，视为修改 |
| `ISDIR` | 目录标记 | 改变处理逻辑为目录级别 |

**⚠️ 重要事实校正：WatchRecordScanner 删除分支的日志语义写反了**

**有问题的代码** (`app/Services/Scanners/WatchRecordScanner.php:59-64`)：
```php
private function handleDeletedFileRecord(string $path): void
{
    $result = $this->songRepository->findOneByPath($path)?->delete();

    // ❌ 三元表达式条件写反！
    Log::info($result === null ? "$path deleted." : "$path doesn't exist in our database--skipping.");
}
```

**真实逻辑与日志输出的错位分析**：

| 实际场景 | `$result` 值 | 实际日志输出 | 期望日志输出 | 错位程度 |
|---------|-------------|-------------|-------------|---------|
| 数据库中存在记录，删除成功 | `non-null`（`delete()` 返回值） | `"$path doesn't exist in our database--skipping."` | `"$path deleted."` | 完全相反 |
| 数据库中不存在记录，跳过删除 | `null`（`findOneByPath()` 返回 null） | `"$path deleted."` | `"$path doesn't exist in our database--skipping."` | 完全相反 |

**根因分析**：
代码使用了 `$result === null ? A : B` 的三元表达式，但对 `$result` 的语义理解错误：
- `$result === null` 的唯一来源是 `findOneByPath()` 返回 null，即**记录不存在**
- `$result !== null` 意味着 `findOneByPath()` 找到了记录并且调用了 `delete()`，即**删除成功**

**对可观测性的影响**：
- ⚠️ **日志语义不可靠**：成功删除显示"跳过"，实际跳过显示"删除"，语义完全颠倒
- ⚠️ **增加故障排查难度**：运维人员排查"文件删除后数据库记录还在"的问题时，日志可能误导方向
- ⚠️ **影响监控告警准确性**：基于日志关键字的监控告警（如 "deleted" 计数）会出现偏差
- ⚠️ **降低审计可信度**：如果有合规要求需要追踪文件删除操作，该日志的证据效力会受到影响

**目录删除日志正常** (`app/Services/Scanners/WatchRecordScanner.php:77-86`)：
```php
private function handleDeletedDirectoryRecord(string $path): void
{
    $count = Song::query()->inDirectory($path)->delete();

    if ($count) {
        Log::info("Deleted $count song(s) under $path"); // ✅ 正确
    } else {
        Log::info("$path is empty--no action needed."); // ✅ 正确
    }
}
```

使用 `inDirectory()` 查询作用域批量删除 (`app/Builders/SongBuilder.php:49-55`)：
```php
public function inDirectory(string $path): self
{
    $path = Str::finish(trim($path), DIRECTORY_SEPARATOR);
    return $this->where('path', 'LIKE', "$path%");
}
```

### 3.5 局限性与优化点

**当前实现的局限性**：
1. **文件移动数据丢失风险**：移动文件可能丢失播放历史、播放列表关联、流派关联等用户数据（通过数据库级联删除）
2. **收藏数据残留**：移动文件后收藏记录变成孤儿，当前实现不会自动清理
3. **空专辑残留可能**：通过 inotify 删除歌曲时不触发 LibraryChanged，空专辑/艺术家可能不会被即时清理
4. **队列异步一致性问题**：MediaScanCompleted 监听器异步执行，PruneLibrary 可能在删除失效路径之前运行
5. **日志语义错误**：handleDeletedFileRecord 的日志条件写反，影响可观测性
6. **hash 字段未充分利用**：虽然计算了文件哈希，但当前未用于文件移动识别或重复检测
7. **inotify 依赖**：当前监听实现主要针对 Linux inotify，Windows/macOS 可能需要额外适配

**建议的优化方向**：

```php
// 优化方案1：修复 WatchRecordScanner 删除日志
private function handleDeletedFileRecord(string $path): void
{
    $song = $this->songRepository->findOneByPath($path);

    if ($song === null) {
        Log::info("$path doesn't exist in our database--skipping.");
        return;
    }

    $song->delete();
    Log::info("$path deleted.");
}

// 优化方案2：在 DeleteNonExistingRecordsPostScan 中增加哈希匹配
public function handle(MediaScanCompleted $event): void
{
    $validPaths = $event->results->valid()->pluck('path')->toArray();
    $cloudPaths = $this->songRepository->getAllStoredOnCloud()->pluck('path')->toArray();
    $allValidPaths = array_merge($validPaths, $cloudPaths);

    // 找出待删除的歌曲
    $songsToDelete = Song::query()
        ->whereNull('podcast_id')
        ->whereNotIn('path', $allValidPaths)
        ->get();

    foreach ($songsToDelete as $song) {
        // 尝试通过哈希匹配找到新路径的歌曲
        $newSong = Song::query()
            ->whereIn('path', $validPaths)
            ->where('hash', $song->hash)
            ->where('id', '!=', $song->id)
            ->first();

        if ($newSong) {
            // 迁移关联数据而不是删除
            $this->migrateSongAssociations($song, $newSong);
            $song->delete();
        } else {
            $song->delete();
        }
    }
}

private function migrateSongAssociations(Song $oldSong, Song $newSong): void
{
    // 迁移播放历史
    DB::table('interactions')
        ->where('song_id', $oldSong->id)
        ->update(['song_id' => $newSong->id]);

    // 迁移播放列表关联
    DB::table('playlist_song')
        ->where('song_id', $oldSong->id)
        ->update(['song_id' => $newSong->id]);

    // 迁移收藏（更新多态关联）
    DB::table('favorites')
        ->where('favoriteable_type', 'playable')
        ->where('favoriteable_id', $oldSong->id)
        ->update(['favoriteable_id' => $newSong->id]);

    // 迁移流派关联
    DB::table('genre_song')
        ->where('song_id', $oldSong->id)
        ->update(['song_id' => $newSong->id]);
}

// 优化方案3：定期清理 favorites 孤儿记录
// 在 PruneLibrary 中增加：
DB::table('favorites')
    ->where('favoriteable_type', 'playable')
    ->whereNotIn('favoriteable_id', function ($query) {
        $query->select('id')->from('songs');
    })
    ->delete();
```

---

## 四、核心类关系图

```
┌─────────────────┐     ┌────────────────────┐
│  ScanCommand    │────▶│ DirectoryScanner   │
└─────────────────┘     └────────────────────┘
          │                        │
          │                        ▼
          │              ┌────────────────────┐
          │              │  ScanConfiguration │
          │              └────────────────────┘
          ▼
┌─────────────────┐     ┌────────────────────┐
│ WatchRecordScanner │  │ IndividualFileHandler │
└─────────────────┘     └────────────────────┘
          │                        │
          ▼                        ▼
┌─────────────────┐     ┌────────────────────┐
│ InotifyWatchRecord │  │    FileScanner     │
└─────────────────┘     └────────────────────┘
                                   │
                                   ▼
                          ┌────────────────────┐
                          │  ScanInformation   │
                          └────────────────────┘
                                   │
                                   ▼
                          ┌────────────────────┐
                          │    SongService     │
                          └────────────────────┘
                                   │
             ┌─────────────────────┼─────────────────────┐
             ▼                     ▼                     ▼
    ┌───────────────┐     ┌───────────────┐     ┌───────────────┐
    │     Song      │     │    Album      │     │    Artist     │
    └───────────────┘     └───────────────┘     └───────────────┘
            │
            ├─ 级联删除（数据库层）
            │   ├─ interactions（ON DELETE CASCADE）
            │   ├─ playlist_song（ON DELETE CASCADE）
            │   └─ genre_song（ON DELETE CASCADE）
            └─ 多态关联（应用层）
                └─ favorites（无外键，孤儿记录）

                          事件监听关系：
┌─────────────────────┐
│ MediaScanCompleted  │─────── 推入队列 ───────┐
└─────────────────────┘                        │
          │                                    │
          ├─ DeleteNonExistingRecordsPostScan │ 异步执行
          ├─ PruneLibrary                     │ 顺序不保证
          └─ WriteScanLog                     │
                                               ▼
                                    ┌────────────────────┐
                                    │  队列处理器（Queue） │
                                    └────────────────────┘

┌─────────────────────┐     ┌────────────────────┐
│ SongService::deleteSongs │─▶│  LibraryChanged   │─▶│ PruneLibrary │
└─────────────────────┘     └────────────────────┘    └───────────────┘

⚠️  WatchRecordScanner 直接 delete，不触发 LibraryChanged
⚠️  handleDeletedFileRecord 日志语义写反
```

---

## 五、关键设计决策总结

| 决策点 | 设计选择 | 权衡考虑 | 代码证据 |
|--------|----------|---------|---------|
| 唯一标识 | 使用绝对路径作为歌曲唯一键 | 实现简单、查询高效，但移动文件可能丢失关联数据 | `SongRepository::findOneByPath()` |
| 修改检测 | 基于 mtime 时间戳比较 | 性能优先，极端情况（同秒修改）可能漏检 | `Song::isFileModified()` |
| 批量删除 | deleteWhereValueNotIn 分批算法 | 解决 SQL IN 子句 65535 元素限制 | `SupportsDeleteWhereValueNotIn` trait |
| 并行扫描 | 多进程 chunk 处理 | 充分利用多核，避免单进程内存溢出 | `ParallelScanStrategy` |
| 关联删除 | interactions/playlist_song/genre_song 使用 ON DELETE CASCADE | 数据一致性，但移动文件时数据可能丢失 | 各表迁移文件中的外键定义 |
| 收藏关联 | 多态关联 + INNER JOIN 从 songs 表查询 | 支持多类型收藏，但删除歌曲后产生孤儿记录 | `FavoriteableBuilder::withFavoriteStatus()` |
| whereHasMorph 使用边界 | 当前未使用 whereHasMorph | 避免误解，当前实现仅用 INNER JOIN | 全代码库搜索无 whereHasMorph 调用 |
| 清理链路边界 | MediaScanCompleted 仅覆盖全量扫描 | 职责清晰，但单记录扫描无批量清理 | `DirectoryScanner.php:46` vs `ScanCommand.php:127` |
| 扫描后清理 | MediaScanCompleted 监听器全部异步队列 | 不阻塞扫描进程，但执行顺序不保证 | 三个监听器均实现 `ShouldQueue` |
| 队列执行顺序 | 数组顺序 ≠ 执行顺序，依连接模式而异 | 并发性能优先，顺序保证取决于队列驱动 | `EventServiceProvider.php:47-51` + `config/queue.php` |
| sync 队列模式 | 严格按数组顺序同步执行 | 一致性最好，但阻塞扫描进程 | `QUEUE_CONNECTION=sync` |
| database 队列模式 | 通常按入队顺序，单 worker 时接近顺序 | 一致性较好，多 worker 可能交错 | `QUEUE_CONNECTION=database`（默认） |
| redis/beanstalkd 队列模式 | 不保证顺序，高并发下不确定性增加 | 性能优先，竞态概率相对较高 | `QUEUE_CONNECTION=redis` |
| sqs 队列模式 | 完全不保证顺序，分布式队列特性 | 云原生友好，依赖最终一致性 | `QUEUE_CONNECTION=sqs` |
| 清理触发 | LibraryChanged 仅在 SongService::deleteSongs() 中触发 | 减少事件触发，但 inotify 删除时空专辑可能残留 | `SongService.php:213` |
| 日志记录 | handleDeletedFileRecord 日志条件写反 | 代码错误，影响可观测性 | `WatchRecordScanner.php:63` |
| 日志语义 | 三元表达式条件倒置，成功显示跳过，跳过显示成功 | 增加故障排查难度，影响监控告警 | `WatchRecordScanner.php:63` 日志分析 |
| 冗余字段 | album_name / artist_name | 以存储空间换取查询性能，避免 JOIN | Song 模型字段 |
| 清理策略 | 实时监听 + 扫描后清理 | 兼顾实时性和最终一致性 | `WatchRecordScanner` + `DeleteNonExistingRecordsPostScan` |
| 云存储处理 | 单独查询云存储路径合并到白名单 | 避免全量扫描时误删云存储歌曲记录 | `DeleteNonExistingRecordsPostScan.php:28` |
| 移动识别 | 当前未实现（hash 字段未充分利用） | 为了实现简单，牺牲了移动文件的数据连续性 | `ScanInformation` 计算 hash 但未用于移动检测 |
