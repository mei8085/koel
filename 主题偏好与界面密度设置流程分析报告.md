# Koel 主题偏好与界面密度设置流程分析报告

## 一、概述

本文档详细分析 Koel 音乐播放器中**主题偏好（Theme Preference）**与**界面密度（View Mode / Layout Density）**两项用户设置，从数据库存储、后端处理、API 传输到前端状态管理与界面渲染的完整端到端流程。

---

## 二、系统架构总览

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              数据流向总图                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   ┌──────────────┐     ┌──────────────────┐     ┌──────────────────────┐   │
│   │  数据库存储   │────▶│   后端 PHP/Laravel│────▶│  前端 Vue 3 + Pinia   │   │
│   │  users表     │     │  模型/仓库/控制器  │     │  Store + Components   │   │
│   └──────────────┘     └──────────────────┘     └──────────────────────┘   │
│         ▲                        ▲                         │                │
│         │                        │                         ▼                │
│   ┌──────────────┐     ┌──────────────────┐     ┌──────────────────────┐   │
│   │  JSON 序列化  │     │  HTTP PATCH API  │◀────│  用户交互 + HTTP 请求  │   │
│   │  preferences │     │  /me/preferences  │     │  UI 控件(切换/选择)    │   │
│   └──────────────┘     └──────────────────┘     └──────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 三、后端存储与数据模型

### 3.1 数据库存储层

用户偏好存储在 `users` 表的 `preferences` 字段中，采用 JSON 格式序列化存储。

| 字段 | 类型 | 说明 |
|------|------|------|
| `preferences` | `TEXT` / `JSON` | 存储所有用户偏好的 JSON 字符串，由 Eloquent Cast 自动转换 |

### 3.2 Eloquent 模型与 Cast

**[User.php](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/app/Models/User.php#L86-L91)** 中通过 `casts()` 方法注册自定义 Cast：

```php
protected function casts(): array
{
    return [
        'preferences' => UserPreferencesCast::class,
    ];
}
```

**[UserPreferencesCast.php](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/app/Casts/UserPreferencesCast.php)** 负责 JSON ↔ 值对象的双向转换：

```php
// 读取时：JSON → UserPreferences 对象
public function get($model, string $key, $value, array $attributes): UserPreferences
{
    return UserPreferences::fromArray(json_decode($value, true) ?: []);
}

// 写入时：UserPreferences 对象 → JSON
public function set($model, string $key, $value, array $attributes): ?string
{
    return json_encode($value ?: []);
}
```

### 3.3 偏好值对象体系

Koel 采用**可扩展的面向对象偏好系统**，每个偏好项对应一个独立的类。

#### 3.3.1 基类 Preference

**[Preference.php](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/app/Values/User/Preferences/Preference.php)** 定义了所有偏好项的契约：

| 方法 | 作用 |
|------|------|
| `getKey()` | 返回 snake_case 格式的存储键名（从类名自动推导，如 `ThemePreference` → `theme`） |
| `getProperty()` | 返回 camelCase 格式的属性访问名（如 `theme`） |
| `getDefaultValue()` | 返回该偏好的默认值 |
| `assert()` | 对值进行校验（可被子类重写） |
| `cast()` | 对输入值进行类型转换 |
| `isCustomizable()` | 是否允许用户自定义（影响 API 可写性） |
| `getAliases()` | 兼容旧版本存储的键名别名 |

#### 3.3.2 主题偏好类

**[ThemePreference.php](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/app/Values/User/Preferences/ThemePreference.php)**：

```php
class ThemePreference extends Preference
{
    public function getDefaultValue(): string
    {
        return 'classic';  // 默认使用经典主题
    }
}
```

#### 3.3.3 界面密度（视图模式）偏好类

界面密度通过三个独立的偏好项分别控制不同实体列表的视图：

- **[AlbumsViewModePreference.php](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/app/Values/User/Preferences/AlbumsViewModePreference.php)**：专辑列表视图
- **[ArtistsViewModePreference.php](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/app/Values/User/Preferences/ArtistsViewModePreference.php)**：艺术家列表视图
- **[RadioStationsViewModePreference.php](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/app/Values/User/Preferences/RadioStationsViewModePreference.php)**：电台列表视图

以专辑视图为例：

```php
class AlbumsViewModePreference extends Preference
{
    public function getDefaultValue(): string
    {
        return 'thumbnails';  // 默认缩略图模式（低密度）
    }

    public function assert(): void
    {
        // 校验：只允许两种值
        Assert::oneOf($this->value, ['list', 'thumbnails']);
    }
}
```

| 取值 | 含义 | 对应布局密度 |
|------|------|------------|
| `thumbnails` | 缩略图网格模式 | 低密度（`full` 布局，卡片竖向，大缩略图） |
| `list` | 列表模式 | 高密度（`compact` 布局，卡片横向，小缩略图） |

#### 3.3.4 UserPreferences 聚合值对象

**[UserPreferences.php](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/app/Values/User/UserPreferences.php)** 是所有偏好项的聚合容器：

- 通过 **反射自动发现** `app/Values/User/Preferences/` 目录下所有 `*Preference.php` 类
- 通过 `__get()` / `__set()` 魔术方法实现属性式访问（如 `$user->preferences->theme`）
- `toArray()` 方法序列化所有偏好为数组，供 JSON 存储
- `set(string $key, mixed $value)` 方法返回新实例（不可变对象风格）

### 3.4 自定义主题模型 Theme

Koel Plus 版本支持用户创建自定义主题，存储在独立的 `themes` 表中：

**[ThemeRepository.php](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/app/Repositories/ThemeRepository.php)** 提供查询方法：

```php
// 查询用户的某个自定义主题
public function findUserThemeById(string $id, User $user): ?Theme
{
    return Theme::query()->whereBelongsTo($user)->find($id);
}
```

---

## 四、数据读取与初始化流程

### 4.1 应用启动入口

前端应用启动链路：

```
[app.ts] createApp(App).mount('#app')
    ↓
[App.vue] onMounted() 检测认证状态
    ↓ 已认证
[AppInitializer.vue] onMounted()
    ↓ 显示加载遮罩
[commonStore.init()]  ← 核心初始化方法
```

**[AppInitializer.vue](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/resources/assets/js/components/utils/AppInitializer.vue#L37-L73)** 关键代码：

```typescript
onMounted(async () => {
  showOverlay({ message: 'Just a little patience…' })
  try {
    await commonStore.init()  // 触发完整初始化
    // ... 其他初始化
    emits('success')
  } finally {
    hideOverlay()
  }
})
```

### 4.2 初始数据 API

**[commonStore.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/resources/assets/js/stores/commonStore.ts#L60-L78)** 的 `init()` 方法调用后端 `/api/data` 接口：

```typescript
async init() {
  Object.assign(this.state, await http.get<CommonStoreState>('data'))

  userStore.init(this.state.current_user)
  preferenceStore.init(this.state.current_user.preferences)  // ① 加载偏好
  // ... 其他 store 初始化
  themeStore.init(this.state.current_theme || themeStore.getCurrentTheme())  // ② 加载主题

  return this.state
}
```

后端由 **[FetchInitialDataController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/app/Http/Controllers/API/FetchInitialDataController.php)** 处理：

```php
// 在返回数据中包含两个关键部分：
return response()->json([
    // ... 其他字段
    'current_user' => UserResource::make($user),      // 含 preferences
    'current_theme' => $theme ? ThemeResource::make($theme) : null,  // 当前自定义主题
]);
```

**[UserResource.php](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/app/Http/Resources/UserResource.php#L49)** 将偏好序列化输出：

```php
'preferences' => $this->when($isCurrentUser, fn () => $this->user->preferences),
```

> 注意：`preferences` 只会返回给当前登录用户本人，不会暴露给其他用户。

### 4.3 preferenceStore 初始化

**[preferenceStore.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/resources/assets/js/stores/preferenceStore.ts#L58-L76)**：

```typescript
init(preferences: UserPreferences = defaultPreferences) {
  // 将后端数据合并到响应式 state
  Object.assign(this.state, preferences)
  // 为每个偏好项建立属性代理（支持 store.theme / store.albums_view_mode 形式访问）
  this.setupProxy()
  this.initialized.value = true
}

setupProxy() {
  Object.keys(this.state).forEach(key => {
    Object.defineProperty(this, key, {
      get: (): any => this.get(key),
      set: (value: any): void => this.set(key, value),
      configurable: true,
    })
  })
}
```

前端的默认偏好定义在同文件的 `defaultPreferences` 中（L4-L50），包含：
- `theme: 'classic'`
- `albums_view_mode: 'thumbnails'`
- `artists_view_mode: 'thumbnails'`
- `radio_stations_view_mode: 'thumbnails'`

### 4.4 themeStore 初始化

**[themeStore.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/resources/assets/js/stores/themeStore.ts#L37-L55)**：

```typescript
init(theme: Theme | Theme['id'] = 'classic') {
  // ① 记录当前 body 上的 CSS 默认属性值
  for (const key in this.defaultProperties) {
    this.defaultProperties[key] = document.body.style.getPropertyValue(key)
  }

  // ② 监听高亮色变化，动态计算前景色（黑/白文字适配）
  const observer = new StyleObserver(([{ value }]) => {
    document.body.style.setProperty(
      '--color-highlight-fg',
      isDarkColor(value) ? '#ffffff' : '#111111'
    )
  })
  observer.observe(document.body, '--color-highlight')

  // ③ 如果是从后端传来的自定义主题，加入主题列表
  if (typeof theme === 'object' && theme.is_custom) {
    this.state.themes.push(theme)
  }

  // ④ 应用主题
  this.setTheme(theme)
}
```

---

## 五、主题偏好驱动前端渲染流程

### 5.1 内置主题配置

**[themes.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/resources/assets/js/config/themes.ts)** 预定义了 18 个内置主题，每个主题包含：

| 字段 | 说明 |
|------|------|
| `id` | 主题唯一标识（如 `classic`, `violet`, `cat`） |
| `name` | 主题显示名称 |
| `thumbnail_color` | 主题预览卡片背景色 |
| `thumbnail_image` | 主题预览缩略图 |
| `properties` | CSS 变量映射（`--color-bg`, `--color-highlight`, `--bg-image` 等） |

### 5.2 主题应用机制

**[themeStore.ts#L61-L78](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/resources/assets/js/stores/themeStore.ts#L61-L78)** 的 `setTheme()` 方法是核心：

```typescript
setTheme(theme?: Theme | Theme['id']) {
  // 解析参数：id → Theme 对象
  if (typeof theme === 'string') {
    theme = this.getThemeById(theme) ?? this.getDefaultTheme()
  }

  // ① 在 html 元素上设置 data-theme 属性（供 CSS 选择器使用）
  document.documentElement.setAttribute('data-theme', theme.id)

  // ② 合并默认属性 + 主题自定义属性
  const properties = { ...this.defaultProperties, ...(theme.properties ?? {}) }

  // ③ 将所有 CSS 变量设置到 body 元素的 style 属性上（覆盖 :root 级变量）
  for (const key in properties) {
    document.body.style.setProperty(key, properties[key])
  }

  // ④ 同步保存偏好（触发 API 更新）
  preferences.theme = theme.id
}
```

**CSS 变量作用链路图：**

```
:root (CSS 全局默认值)
    ↓
document.body.style.setProperty()  ← 更高优先级，用户主题覆盖
    ↓
所有组件通过 Tailwind 自定义类（k-bg, k-fg, k-highlight 等）引用 CSS 变量
```

### 5.3 主题选择 UI

用户通过 **[ThemePreferences.vue](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/resources/assets/js/components/profile-preferences/theme/ThemePreferences.vue)** 界面选择主题：

```
ThemePreferences.vue
    ├─ ThemeList.vue  (显示主题网格)
    │     └─ ThemeCard.vue  (单个主题卡片)
    │           └─ 点击 → themeStore.setTheme(theme)
    └─ CreateThemeForm.vue  (Plus 用户自定义主题)
```

**[ThemeCard.vue#L39](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/resources/assets/js/components/profile-preferences/theme/ThemeCard.vue#L39)**：

```typescript
const onClick = () => themeStore.setTheme(theme.value)
```

---

## 六、界面密度（View Mode）驱动前端渲染流程

### 6.1 视图模式切换控件

**[ViewModeSwitch.vue](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/resources/assets/js/components/ui/ViewModeSwitch.vue)** 是统一的视图切换组件，通过 `defineModel` 实现与父组件的双向绑定：

```vue
<!-- 缩略图模式按钮 -->
<input v-model="value" type="radio" value="thumbnails" />

<!-- 列表模式按钮 -->
<input v-model="value" type="radio" value="list" />
```

### 6.2 列表页应用流程（以 AlbumListScreen 为例）

**[AlbumListScreen.vue](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/resources/assets/js/components/screens/AlbumListScreen.vue)**：

**① 控件绑定（L27）：**
```vue
<ViewModeSwitch v-model="preferences.albums_view_mode" />
```

**② 计算布局模式（L112-L113）：**
```typescript
// view_mode → 卡片布局密度
const itemLayout = computed<CardLayout>(() =>
  preferences.albums_view_mode === 'thumbnails' ? 'full' : 'compact'
)

// 不同密度下网格最小列宽不同
const minItemWidth = computed(() =>
  preferences.albums_view_mode === 'thumbnails' ? 240 : 350
)
```

**③ 传递给卡片组件（L70-L74）：**
```vue
<AlbumCard
  :album="item"
  :layout="itemLayout"
  :show-release-year="preferences.albums_sort_field === 'year'"
/>
```

### 6.3 卡片布局密度实现

密度渲染的核心在 **[AlbumOrArtistCard.vue](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/resources/assets/js/components/ui/album-artist/AlbumOrArtistCard.vue)**：

**① HTML 结构（L7-L11）：**
```vue
<WithGradientBorder :class="{ compact: layout === 'compact' }">
  <article :class="layout" class="relative group flex p-5 rounded-[inherit] flex-col gap-5">
```

**② 低密度 `full` 模式（默认）：**
- 卡片内边距 `p-5`，元素间距 `gap-5`
- 缩略图大，内容竖向排列
- 播放图标放大 3 倍（`:deep(.play-icon) { @apply scale-[3] }`）

**③ 高密度 `compact` 模式（L91-L97）：**
```postcss
&.compact {
  @apply flex-row gap-4 p-3 rounded-md items-center;  /* 横向排列，间距更小 */

  :deep(.thumbnail) {
    @apply w-[80px] rounded-md;  /* 缩略图固定 80px 宽度 */
  }
}
```

**两种布局视觉对比：**

| 特性 | `full` 低密度（thumbnails） | `compact` 高密度（list） |
|------|---------------------------|------------------------|
| 布局方向 | `flex-col` 竖向 | `flex-row` 横向 |
| 内边距 | `p-5` (20px) | `p-3` (12px) |
| 元素间距 | `gap-5` (20px) | `gap-4` (16px) |
| 缩略图尺寸 | 默认（大） | `w-[80px]` 固定小 |
| 卡片最小宽 | `240px` | `350px` |
| 网格间距 | `gap-y-5` | `gap-y-3` |
| 单位面积卡片数 | 少（稀疏） | 多（紧凑） |

### 6.4 其他列表页的密度控制

相同模式同时应用于多个屏幕：

| 屏幕组件 | 偏好键 |
|---------|--------|
| `AlbumListScreen.vue` | `albums_view_mode` |
| `ArtistListScreen.vue` | `artists_view_mode` |
| `RadioStationListScreen.vue` | `radio_stations_view_mode` |
| 首页 `TopAlbums.vue` / `NewAlbums.vue` 等 | 直接使用固定布局 |

---

## 七、偏好修改保存流程

### 7.1 写入链路总览

```
用户操作 UI (点击/切换)
    ↓
Vue 响应式赋值（如 preferences.theme = 'violet'）
    ↓
preferenceStore.set()  触发
    ↓
preferenceStore.update()  →  HTTP PATCH /api/me/preferences
    ↓
UpdateUserPreferenceController::__invoke()
    ↓
UserService::savePreference()  →  $user->save()
    ↓
UserPreferencesCast::set()  →  JSON 序列化写入 users.preferences 字段
```

### 7.2 前端 Store 写入逻辑

**[preferenceStore.ts#L78-L102](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/resources/assets/js/stores/preferenceStore.ts#L78-L102)**：

```typescript
set(key: keyof UserPreferences, value: any) {
  // 值相同时不做处理，避免不必要的请求
  if (this.state[key] === value) {
    return
  }

  this.state[key] = value  // ① 先更新本地状态（界面立即响应）

  if (!this._temporary) {
    this.update(key, value)  // ② 异步同步到后端
  }
}

async update(key: keyof UserPreferences, value: any) {
  // silently 表示静默请求，不触发全局 loading/错误提示
  await http.silently.patch('me/preferences', { key, value })

  // 特殊处理：切换公共媒体可见性后需要刷新页面
  if (key === 'include_public_media') {
    window.location.reload()
  }
}
```

> **设计亮点**：采用「先本地更新，后异步同步」的乐观更新模式，UI 无延迟，用户体验流畅。

### 7.3 后端 API 处理

**① 路由定义** — [api.base.php#L211](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/routes/api.base.php#L211)：
```php
Route::patch('me/preferences', UpdateUserPreferenceController::class);
```

**② 表单验证** — [UpdateUserPreferencesRequest.php](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/app/Http/Requests/API/UpdateUserPreferencesRequest.php)：
```php
public function rules(): array
{
    return [
        'key' => ['required', 'string', new CustomizableUserPreference()],
        'value' => 'sometimes',
    ];
}
```

**③ 偏好键合法性校验** — [CustomizableUserPreference.php](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/app/Rules/CustomizableUserPreference.php)：
```php
public function validate(string $attribute, mixed $value, Closure $fail): void
{
    if (!UserPreferences::customizable($value)) {
        $fail('Invalid or uncustomizable user preference key.');
    }
}
```

该规则调用 `UserPreferences::customizable()`，检查：
- 键名是否对应一个存在的 Preference 类
- 该类的 `isCustomizable()` 是否返回 `true`

**④ 控制器处理** — [UpdateUserPreferenceController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/app/Http/Controllers/API/UpdateUserPreferenceController.php)：
```php
public function __invoke(UpdateUserPreferencesRequest $request, UserService $service, Authenticatable $user)
{
    $service->savePreference($user, $request->key, $request->value);
    return response()->noContent();
}
```

**⑤ 服务层落库** — [UserService.php#L99-L104](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/app/Services/UserService.php#L99-L104)：
```php
public function savePreference(User $user, string $key, mixed $value): void
{
    // UserPreferences 采用不可变风格，set() 返回新实例
    $user->preferences = $user->preferences->set($key, $value);
    $user->save();  // 触发 UserPreferencesCast::set() → JSON 序列化
}
```

---

## 八、关键文件索引

### 后端 PHP

| 文件 | 作用 |
|------|------|
| [User.php](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/app/Models/User.php) | 用户模型，注册 preferences cast |
| [UserPreferencesCast.php](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/app/Casts/UserPreferencesCast.php) | JSON ↔ UserPreferences 转换 |
| [UserPreferences.php](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/app/Values/User/UserPreferences.php) | 偏好聚合容器，反射发现 + 属性访问 |
| [Preference.php](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/app/Values/User/Preferences/Preference.php) | 偏好基类 |
| [ThemePreference.php](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/app/Values/User/Preferences/ThemePreference.php) | 主题偏好（默认 `classic`） |
| [AlbumsViewModePreference.php](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/app/Values/User/Preferences/AlbumsViewModePreference.php) | 专辑视图密度（默认 `thumbnails`） |
| [ArtistsViewModePreference.php](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/app/Values/User/Preferences/ArtistsViewModePreference.php) | 艺术家视图密度 |
| [FetchInitialDataController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/app/Http/Controllers/API/FetchInitialDataController.php) | `/api/data` 初始数据接口 |
| [UpdateUserPreferenceController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/app/Http/Controllers/API/UpdateUserPreferenceController.php) | `PATCH /me/preferences` 更新接口 |
| [UpdateUserPreferencesRequest.php](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/app/Http/Requests/API/UpdateUserPreferencesRequest.php) | 偏好更新请求验证 |
| [CustomizableUserPreference.php](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/app/Rules/CustomizableUserPreference.php) | 偏好键合法性校验规则 |
| [UserService.php](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/app/Services/UserService.php) | `savePreference()` 保存逻辑 |
| [UserResource.php](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/app/Http/Resources/UserResource.php) | 用户资源序列化（含 preferences） |
| [ThemeRepository.php](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/app/Repositories/ThemeRepository.php) | 自定义主题查询 |

### 前端 Vue/TS

| 文件 | 作用 |
|------|------|
| [app.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/resources/assets/js/app.ts) | 应用入口，创建 Vue 实例 |
| [App.vue](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/resources/assets/js/App.vue) | 根组件，路由 + 认证检测 |
| [AppInitializer.vue](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/resources/assets/js/components/utils/AppInitializer.vue) | 初始化编排，调用 commonStore.init() |
| [commonStore.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/resources/assets/js/stores/commonStore.ts) | 初始数据加载，编排各 store 初始化 |
| [preferenceStore.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/resources/assets/js/stores/preferenceStore.ts) | 偏好状态管理，读写/同步 API |
| [themeStore.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/resources/assets/js/stores/themeStore.ts) | 主题状态管理，CSS 变量注入 |
| [themes.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/resources/assets/js/config/themes.ts) | 18 个内置主题配置 |
| [useLocalStorage.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/resources/assets/js/composables/useLocalStorage.ts) | 命名空间化的 localStorage 封装 |
| [ThemePreferences.vue](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/resources/assets/js/components/profile-preferences/theme/ThemePreferences.vue) | 主题偏好设置页面 |
| [ThemeCard.vue](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/resources/assets/js/components/profile-preferences/theme/ThemeCard.vue) | 单个主题选择卡片 |
| [ViewModeSwitch.vue](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/resources/assets/js/components/ui/ViewModeSwitch.vue) | 视图密度切换开关 |
| [AlbumListScreen.vue](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/resources/assets/js/components/screens/AlbumListScreen.vue) | 专辑列表页（密度应用示例） |
| [AlbumCard.vue](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/resources/assets/js/components/album/AlbumCard.vue) | 专辑卡片组件 |
| [AlbumOrArtistCard.vue](file:///d:/fz/0601-1/solo-dogfeeding/code/46-koel/resources/assets/js/components/ui/album-artist/AlbumOrArtistCard.vue) | 通用卡片组件，实现 full/compact 密度 |

---

## 九、设计特点总结

### 9.1 架构优点

1. **偏好类即插件**：新增偏好只需在 `Preferences/` 目录添加一个 `*Preference.php` 文件，反射机制自动发现注册，零侵入。

2. **乐观更新策略**：先写本地响应式状态再异步同步后端，UI 操作零延迟。

3. **CSS 变量主题系统**：通过 `document.body.style.setProperty()` 动态注入 CSS 变量，不依赖预编译，支持运行时自定义主题。

4. **不可变值对象**：后端 `UserPreferences::set()` 返回新实例，减少副作用，便于状态追踪。

5. **安全校验闭环**：`CustomizableUserPreference` 验证规则确保只有标记为可定制的偏好才能通过 API 修改，防止越权写入。

6. **属性代理访问**：前端 `preferenceStore.setupProxy()` 让 store 实例本身可通过属性直接访问偏好（`preferences.theme` 而非 `preferences.state.theme`）。

### 9.2 可扩展点

- 可新增更多 Preference 子类实现更丰富的校验和默认值
- 自定义主题（Koel Plus 特性）通过独立 `themes` 表管理，可扩展为系统级预设主题
- `view_mode` 目前是二选一，未来可扩展为更多密度级别
